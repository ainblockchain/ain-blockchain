name: AIN-blockchain CI/CD

on:
  push:
    branches:
      - 'develop'
      - 'release/*'
      - 'master'
  pull_request:
    branches:
      - 'develop'
      - 'release/*'
      - 'master'
jobs:
  performance_test:
    if: ${{ github.event_name == 'pull_request' && github.event.action == 'close' && github.event.pull_request.merged == 'true' && github.event.pull_request.base.ref == 'master' }}
    runs-on: ubuntu-latest
    steps:
      - name: Triggered Test
        run: echo "Triggered"
